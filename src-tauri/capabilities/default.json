{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Amaara Studio window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default"
  ]
}
