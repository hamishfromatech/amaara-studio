{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Navya Studio window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default"
  ]
}
